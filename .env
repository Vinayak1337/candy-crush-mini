# Webpack 4 (react-scripts 4) trips over the source-map minifier's
# lib/mappings.wasm on newer Node versions. We don't ship source maps for this
# small game, so disable them to keep `npm run build` working everywhere.
GENERATE_SOURCEMAP=false
